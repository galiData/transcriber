import React from 'react';

const TranscriptionConfig = ({ 
  numSpeakers, 
  setNumSpeakers, 
  language, 
  setLanguage,
  disabled 
}) => {
  return null; // Hidden - using default Hebrew language and multi-speaker detection
};

export default TranscriptionConfig;